# SmartQuery 项目简历素材

## 项目简介
SmartQuery — 基于 LangGraph 的智能问答系统，采用 Agentic RAG 架构，支持混合检索（稠密+稀疏）、重排序、文档清洗、多轮对话。

## 核心技术栈
- **框架**：LangGraph（有状态工作流编排）、LangChain
- **嵌入模型**：qwen3.7-text-embedding（2048维）+ BGE-M3（稀疏向量）
- **重排序**：BGE-Reranker-v2-M3（CrossEncoder）
- **向量数据库**：Milvus（IVF_FLAT + SPARSE_INVERTED_INDEX）
- **后端**：FastAPI + SSE 流式输出
- **前端**：Streamlit

## 量化成果（简历亮点）

### 检索质量提升
| 指标 | 基线 | 优化后 | 提升 |
|---|---|---|---|
| Recall@5（纯稠密） | 0.646 | **0.822** | **+27.2%** |
| Recall@5（混合检索） | 0.686 | **0.839** | **+22.3%** |
| Recall@5（重排序后） | 0.698 | **0.812** | **+16.3%** |
| nDCG@5 | 0.805 | **0.818** | +1.6% |

### 分层提升（最难的语义改写层）
| 问题类型 | 基线 | 优化后 | 提升 |
|---|---|---|---|
| C_语义改写（口语化查询） | 0.272 | **0.693** | **+154.8%** |
| D_多跳/跨文档 | 0.255 | **0.647** | **+153.7%** |
| A_事实单跳 | 0.886 | **0.964** | +8.8% |

### 技术优化点
1. **Embedding 模型升级**：text-embedding-v4 → qwen3.7-text-embedding，向量维度 1024→2048，指令遵循能力提升 16.4%
2. **混合检索优化**：RRF 融合系数 k=60→30，排名靠前文档权重更大
3. **文档清洗**：去除控制字符、特殊字符、页眉页脚、过短噪声块
4. **分块策略优化**：标题感知分块 + 句子级分块，父块 800 字符，子块 150 字符
5. **检索缓存**：5 分钟 TTL + 线程安全，避免重复计算
6. **工作流精简**：LangGraph 7→4 节点，删除冗余的 LLM 打分/改写/反思循环

## 简历写法建议

### 一句话描述
基于 LangGraph 构建 Agentic RAG 智能问答系统，通过混合检索（稠密+稀疏）、重排序、文档清洗等优化，Recall@5 从 0.646 提升至 0.822（+27.2%），语义改写层提升 154.8%。

### 项目详情
- 设计 4 节点 LangGraph 工作流（意图分析→检索→生成→直接回答），支持条件分支和多轮对话
- 实现稠密（qwen3.7-text-embedding 2048维）+ 稀疏（BGE-M3）混合检索，RRF 融合 + CrossEncoder 重排序
- 实现文档清洗管线（控制字符、页眉页脚、特殊字符过滤）和标题感知分块策略
- 搭建 FastAPI + Streamlit 全栈应用，支持 SSE 流式输出和 Session 管理
- 基于 49 题标注集进行 5 层分级评估（事实单跳/术语/语义改写/多跳/易混淆），量化验证优化效果
